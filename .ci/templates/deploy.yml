parameters:
  deployment: ""
  pool: ""
  environment: ""
  azureSubscription: ""

jobs:
- deployment: ${{ parameters.deployment }}
  displayName: Deploy to ${{ parameters.environment }}
  pool:
    vmImage: ${{ parameters.pool }}
  continueOnError: false
  environment: ${{ parameters.environment }}
  strategy: 
    runOnce:
      deploy:
        steps:
        - checkout: self
          displayName: Get sources

        - task: DownloadBuildArtifacts@0
          displayName: 'Download artifact'
          inputs:
            buildType: 'current'
            downloadType: 'single'
            artifactName: '${{ parameters.environment }}'
            downloadPath: '$(System.ArtifactsDirectory)'
            
        - task: AzureCLI@1
          displayName: 'Deploy task'
          inputs:
            azureSubscription: '${{ parameters.azureSubscription }}'
            addSpnToEnvironment: true
            scriptLocation: inlineScript
            inlineScript: "pwsh .ci/Invoke-PipelineTask.ps1 -azureDevOps -deploy -tfVersion $(tfVersion) -environmentShort ${{ parameters.environment }} -artifactPath \"$(System.ArtifactsDirectory)\"""