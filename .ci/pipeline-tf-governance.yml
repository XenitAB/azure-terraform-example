variables:
  azureSubscription: 'azureRm'
  tfVersion: "0.12.16"
  vmImage: 'ubuntu-latest'
  tfFolderName: 'tf-governance'

name: $(Build.BuildId)

trigger:
  batch: true
  branches:
    include:
      - master
  paths:
    include:
      - tf-governance

stages:
  - template: templates/pipeline.yml
