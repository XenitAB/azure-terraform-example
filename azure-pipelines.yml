variables:
  azureSubscription: "azureRm"
  tfVersion: "0.12.16"
  pool: 'ubuntu-latest'

name: $(Build.BuildId)

trigger:
- uat

stages:
  - template: .ci/templates/build-stages.yml
  - template: .ci/templates/deploy-stages.yml